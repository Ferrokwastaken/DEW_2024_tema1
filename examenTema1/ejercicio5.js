//Israel Duque 2º DAW, Desarrollo en entorno cliente(DEW)
function countNumbers(...numbersList) {
    let totalAmount = new Array(9);
    totalAmount.fill(0);

    for (let i = 0; i < numbersList.length; i++) {
        
    }

    return totalAmount;
}

console.log(countNumbers());